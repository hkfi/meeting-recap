name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify release version
        run: scripts/verify-release-version.sh "$GITHUB_REF_NAME"

      - name: Install xcodegen
        run: brew install xcodegen

      - name: Run tests
        run: make test

      - name: Build
        run: ./build.sh

      - name: Zip app bundle
        run: |
          cd .build
          ditto -c -k --sequesterRsrc --keepParent MeetingRecap.app MeetingRecap.app.zip

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: .build/MeetingRecap.app.zip
          generate_release_notes: true
